{
  "compilerOptions": {
    "target": "ES2020", // 최신 JavaScript 기능 사용
    "module": "ESNext", // ES Modules 사용
    "lib": ["DOM", "ESNext"], // DOM 및 최신 ES 기능 라이브러리 포함
    "outDir": "./dist", // 컴파일된 JavaScript 파일이 저장될 폴더
    "rootDir": "./src", // TypeScript 소스 코드가 있는 폴더
    "strict": true, // 엄격한 타입 검사 활성화 (권장)
    "esModuleInterop": true, // CommonJS와 ES Modules 간의 호환성 활성화
    "skipLibCheck": true, // 라이브러리 타입 검사 건너뛰기
    "forceConsistentCasingInFileNames": true // 파일 이름 대소문자 일관성 강제
  },
  "include": ["src/**/*.ts"], // 컴파일할 TypeScript 파일 지정
  "exclude": ["node_modules", "dist", "src/content/types.ts"] // 컴파일에서 제외할 폴더 지정
}
